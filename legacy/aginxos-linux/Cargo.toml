[workspace]
resolver = "2"
members = [
    "aginx-daemon",
]

[workspace.package]
version = "0.1.0"
edition = "2021"
authors = ["Aginx Team"]
license = "MIT"

[profile.release]
opt-level = "s"
lto = true
strip = true
panic = "abort"
