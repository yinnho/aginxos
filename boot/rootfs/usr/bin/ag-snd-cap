#!/bin/sh
# ag:summary=capture raw audio from a PCM device (M18 listen path, S16_LE)
# ag:args=<pcm-dev> <secs> <outfile> [rate] [chans]
# ag:examples=/dev/snd/pcmC0D0c 3 /tmp/cap.raw
# ag:group=snd
# ag:exec=snd-cap
exec /bin/snd-cap "$@"
