#!/bin/sh
# ag:summary=pm8xxx RTC clock read/set/arm/sync (M23b suspend wake path)
# ag:args=bare reads clock | sync | set epoch | arm +delta|epoch
# ag:examples=sync
# ag:examples=arm +600
# ag:group=sys
# ag:exec=rtcal
exec /bin/rtcal "$@"
